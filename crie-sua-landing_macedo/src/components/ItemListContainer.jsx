import React from "react";

const ItemListContainer = ({ greeting }) => {
  return (
    <section>
      <h2>{greeting}</h2>
      {/* Catálogo de produtos vai aqui futuramente */}
    </section>
  );
};

export default ItemListContainer;
